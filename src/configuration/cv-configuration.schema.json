{
  "type": "object",
  "properties": {
    "NODE_ENV": {
      "type": "string",
      "enum": ["production", "development", "test"]
    },
    "POSTGRES_URI": {
      "type": "string",
      "pattern": "^postgres://"
    }
  },
  "required": ["NODE_ENV", "POSTGRES_URI"]
}
